version: '3'

services:

  backend:

    build: .

    ports:
      - "5000:5000"

    environment:

      PORT: 5000

      DB_HOST: host.docker.internal

      DB_USER: admin

      DB_PASSWORD: BuildSmart@123

      DB_NAME: buildsmart